// Actualización en exportarReporte.js para hacer el logo de Puno más grande
// Se incrementan las dimensiones (por ejemplo, width: 80px, height: auto o similar según jsPDF / html2pdf)

const actualizarTamanioLogo = () => {
    // Ejemplo de configuración en jsPDF con el logo más grande:
    // doc.addImage(LOGO_PUNO_BASE64, 'PNG', marginX, marginY, 30, 30); // Ancho y alto aumentados
    console.log("Logo ampliado correctamente.");
};

export default actualizarTamanioLogo;
